#include<stdio.h>
void main()
{
	int n=5,sum=0,c,val;
	int max=0;
	printf("enter the numbers:");
	for(c=1;c<=n;c++)
	{
		scanf("%d",&val);
		if(val<max,c<=n)
		
			max=val;
		
		
	else
	max=val;
	
	}
	printf("maximum:%d",max);
}
